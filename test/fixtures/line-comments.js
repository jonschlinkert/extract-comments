/**
 * Initialize App.
 *
 *   - setup default configuration
 *   - setup default middleware
 *
 * @api private
 */

App.prototype.foo = function(options) {
};

// this
// is
// a
// multiline
// line
// comment
var str = '// foo bar'

/**
 * Expose `App`
 */

module.exports = App;
